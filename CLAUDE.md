# CLAUDE.md

## Generated calendar data

`calendar_gen.go` is generated — DO NOT EDIT it manually.

Regenerate it by running the generator in the `gen/` subpackage (it fetches the
Cabinet Office CSV over the network):

```bash
cd gen && go generate   # or, from the repo root: go generate ./gen
```
